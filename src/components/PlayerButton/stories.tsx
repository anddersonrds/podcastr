import { Story, Meta } from '@storybook/react/types-6-0'

import PlayerButton from '.'

export default {
  title: 'PlayerButton',
  component: PlayerButton
} as Meta

export const Default: Story = () => <PlayerButton />
